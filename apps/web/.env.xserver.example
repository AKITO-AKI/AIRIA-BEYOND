# Xserver deployment build-time config for Vite
#
# Usage:
# 1) Copy this file to: .env.xserver.local
# 2) Edit values as needed
# 3) Run: npm run build:xserver (or from repo root: npm run build:xserver)

# Where the frontend is hosted.
# - If you upload the built site to the domain root (public_html): use '/'
# - If you host under a subdirectory (public_html/app): use '/app/'
VITE_PUBLIC_BASE_PATH=/

# Backend API base URL (Render, etc.)
# Must be https:// when the site is served over HTTPS.
VITE_API_BASE_URL=https://airia-beyond.onrender.com
