{
  "name": "airia-beyond",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm --workspace apps/web run dev\" \"vercel dev --listen 3000\"",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "vercel dev --listen 3000",
    "build": "npm --workspace apps/web run build",
    "preview": "npm --workspace apps/web run preview",
    "lint": "echo \"TODO: add lint\"",
    "format": "echo \"TODO: add format\""
  },
  "dependencies": {
    "@vercel/node": "^5.5.26",
    "midi-writer-js": "^3.1.1",
    "openai": "^4.77.0",
    "replicate": "^1.4.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^9.2.1",
    "vercel": "^50.4.9"
  }
}
