# Server-side environment variables (for VPS / systemd)
# Copy to /opt/airia-beyond/.env

# Port for Express (optional; default may be 3000 depending on server.js)
PORT=3000
NODE_ENV=production

# Auth/admin
ADMIN_TOKEN=replace-with-long-random

# Auth
# - Password auth is enabled by default unless explicitly disabled.
# - OAuth is disabled by default unless explicitly enabled.
AUTH_ALLOW_PASSWORD=true
AUTH_ALLOW_OAUTH=false

# Recommended for public release: require email for password accounts.
AUTH_REQUIRE_EMAIL=true

# Strongly recommended: set a long random pepper and NEVER rotate/remove it without a migration plan.
# This is used in addition to per-user salt.
AUTH_PASSWORD_PEPPER=replace-with-long-random

# Optional: persist file-based auth store to a specific path (when DATABASE_URL is not used).
# AUTH_STORE_PATH=/opt/airia-beyond/data/auth-store.json

# Postgres (Neon recommended). IMPORTANT: no surrounding quotes.
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# CORS
# The primary public frontend URL (used to generate allowed origins)
APP_PUBLIC_URL=https://app.example.com
# Additional origins, comma-separated. Add both with/without www if needed.
APP_ALLOWED_ORIGINS=https://app.example.com,https://example.com

# Optional: API public base (if you have any absolute URL needs)
# API_PUBLIC_URL=https://api.example.com

# Optional integrations (only if you use them)
# OPENAI_API_KEY=
# REPLICATE_API_TOKEN=

# AI quality tuning (optional)
# Analysis (IR)
OPENAI_MODEL_ANALYSIS=gpt-4o-mini
LLM_TEMPERATURE_ANALYSIS=0.35

# Music
OPENAI_MODEL_MUSIC=gpt-4o-mini
LLM_TEMPERATURE_MUSIC=0.65

# Image prompt style (optional)
# IMAGE_DEFAULT_STYLE_PRESET=white-world
# IMAGE_WHITE_WORLD_OVERLAY=true
